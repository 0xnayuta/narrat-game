/**
 * Responsibility: Placeholder module marker for a future save domain split.
 * Status: Not connected to the active demo path. Current persistence logic lives in engine/save
 * and is consumed directly by the demo UI.
 * TODO: Revisit only if save/persistence becomes a first-class runtime module.
 */

export const SAVE_MODULE = "save";

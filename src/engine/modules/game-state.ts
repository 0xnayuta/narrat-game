/**
 * Responsibility: Placeholder module marker for a future game-state domain split.
 * Status: Not connected to the active demo path. Current state logic lives in
 * engine/state/GameState.ts and related runtime helpers.
 * TODO: Only keep if/when module-level domain registration becomes real.
 */

export const GAME_STATE_MODULE = "game-state";
